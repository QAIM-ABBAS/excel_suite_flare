import { serve } from "bun";

// This is a placeholder - the actual Python FastAPI server runs via uvicorn
// We use this file only if bun is needed for something else
console.log("API service is managed by Python FastAPI - see README");
